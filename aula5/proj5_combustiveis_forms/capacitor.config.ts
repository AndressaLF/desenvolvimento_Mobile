import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proj5_combustiveis_forms',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
